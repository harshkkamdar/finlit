// Twitter card uses the same image as Open Graph (1200x630 summary_large_image).
// Re-exporting from opengraph-image keeps a single source of truth.
export { default, alt, size, contentType } from "./opengraph-image";
